{
  "compilerOptions": {
    /* Basic Options */
    "target": "es2021",                          /* Specify ECMAScript target version */
    "module": "commonjs",                        /* Specify module code generation */
    "lib": ["es2021"],                           /* Specify library files to be included. */
    "outDir": "./dist",                          /* Redirect output structure to the directory. */
    "rootDir": "./src",                          /* Specify the root directory of input files. */
    "declaration": true,                         /* <-- CRUCIAL: Generates corresponding '.d.ts' files. */
    "declarationMap": true,                      /* Generates a source map for each '.d.ts' file. */
    "sourceMap": true,                           /* Generates corresponding '.js.map' files. */
    "composite": true,                           /* Enable project compilation. Important for monorepos. */

    /* Strict Type-Checking Options */
    "strict": true,                              /* Enable all strict type-checking options. */
    "esModuleInterop": true,                     /* Enables emit interoperability between CommonJS and ES Modules. */

    /* Module Resolution Options */
    "moduleResolution": "node",                  /* Specify module resolution strategy. */
    
    /* Advanced Options */
    "skipLibCheck": true,                        /* Skip type checking of all declaration files (*.d.ts). */
    "forceConsistentCasingInFileNames": true     /* Disallow inconsistently-cased references to the same file. */
  },
  "include": ["src"],                           /* Include all files in the src directory */
  "exclude": ["node_modules", "dist"]           /* Exclude node_modules and the output directory */
}